//1. zadatak a)
CHIP zadatak1a {
    IN x, y;
    OUT out;

    PARTS:
    
    Not(in = y, out = notY);
    Not(in = x, out = notX);
    
    
   

    
    Or(a = notX, b = notX, out = out);
}

